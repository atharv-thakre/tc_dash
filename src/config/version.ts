/**
 * Centralized Site and Engine Version Configuration
 *
 * Change SITE_VERSION here to update the version across all pages and components:
 * - Login Page
 * - Signup Page
 * - Landing Page (Nav, Hero Pill, Footer)
 * - Dashboard & Admin Pages (Header Navbar, TcAuthLogo, Interactive Terminal, Comparison Matrix, Showcase)
 */
export const SITE_VERSION = '1.5.2';
export const SITE_VERSION_LABEL = `v${SITE_VERSION}`;
export const AUTH_ENGINE_VERSION = SITE_VERSION;
export const AUTH_ENGINE_VERSION_TAG = `tc_auth_v${SITE_VERSION}`;
